---
title: Hooks
description: Auth-first hook guides for form state, resend timing, and screen orchestration.
---

import Link from '@docusaurus/Link';

<div className="doc-hero doc-hero--compact">
  <span className="doc-kicker">Reference Guide</span>
  <h1>The hook layer explains how auth behavior is assembled, not just where it lives.</h1>
  <p className="doc-summary">
    Each hook page answers two questions: what the hook is doing, and why the codebase keeps that behavior
    in a hook instead of inside the screen component directly.
  </p>
</div>

<div className="doc-diagram">

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'fontFamily': 'Inter, sans-serif', 'primaryColor': '#eff6ff', 'primaryTextColor': '#0f172a', 'primaryBorderColor': '#2563eb', 'secondaryColor': '#f8fafc', 'tertiaryColor': '#ffffff', 'lineColor': '#94a3b8' }}}%%
flowchart LR
  A["AuthForm"] --> B["useAuthForm"]
  B --> C["useAuthFormState"]
  C --> D["AUTH_FORM_INPUT_CONFIG"]
  C --> E["Zod schema"]
  B --> F["useAuth()"]
  G["VerifyEmailForm"] --> H["useVerifyEmail"]
  H --> C
  H --> I["useResendTimer"]
  J["RequestPasswordResetForm"] --> K["useRequestPasswordReset"]
  K --> C
  L["VerifyPasswordResetForm"] --> M["useVerifyPasswordReset"]
  M --> C
  M --> I
```

</div>

<div className="doc-grid doc-grid--2">
  <Link className="doc-card doc-card--link" to="/docs/hooks/use-auth-form-state">
    <span className="doc-cardEyebrow">Shared</span>
    <h2>useAuthFormState</h2>
    <p>Builds the reusable React Hook Form and Zod state that the auth screens depend on.</p>
  </Link>
  <Link className="doc-card doc-card--link" to="/docs/hooks/use-resend-timer">
    <span className="doc-cardEyebrow">Shared</span>
    <h2>useResendTimer</h2>
    <p>Owns the cooldown clock used by OTP resend flows.</p>
  </Link>
  <Link className="doc-card doc-card--link" to="/docs/hooks/use-auth-form">
    <span className="doc-cardEyebrow">Screen</span>
    <h2>useAuthForm</h2>
    <p>Coordinates login and registration submit behavior for the shared auth form shell.</p>
  </Link>
  <Link className="doc-card doc-card--link" to="/docs/hooks/use-request-password-reset">
    <span className="doc-cardEyebrow">Screen</span>
    <h2>useRequestPasswordReset</h2>
    <p>Owns the request-reset workflow from email entry through redirect to the verify step.</p>
  </Link>
  <Link className="doc-card doc-card--link" to="/docs/hooks/use-verify-email">
    <span className="doc-cardEyebrow">Screen</span>
    <h2>useVerifyEmail</h2>
    <p>Combines verification submit logic, resend logic, timer state, and redirect behavior.</p>
  </Link>
  <Link className="doc-card doc-card--link" to="/docs/hooks/use-verify-password-reset">
    <span className="doc-cardEyebrow">Screen</span>
    <h2>useVerifyPasswordReset</h2>
    <p>Runs the password-reset verification and resend workflow that the recovery flow depends on.</p>
  </Link>
</div>
